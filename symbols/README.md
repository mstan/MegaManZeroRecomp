# symbols/

Optional symbol and boundary metadata belongs here. Symbols are discovery aids,
not execution truth. The initial bring-up deliberately starts from the ROM entry
and the engine's static finder; reviewed miss proposals may add irreducible
entries to `game.toml` while class-level discovery gaps are fixed in gbarecomp.
